## Plugin versioning

Bump the version in `.claude-plugin/plugin.json` for any change to the skill (`skills/` directory).

## Committing

Proactively commit and push completed work without waiting to be asked.
